// Preload isolado apenas para futuras integrações. Mantemos sem exposições para evitar APIs supérfluas.
window.addEventListener('DOMContentLoaded', () => {
  // Nenhuma ponte necessária: toda a lógica vive em app/assets/main.js.
});
